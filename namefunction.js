var b=function addNumbers(num,num1) {
    // code to be executed
    console.log(num+num1)
}
addNumbers(6,10);

// o/p:-16
// hints:-In this function we call the function name that is called name function.