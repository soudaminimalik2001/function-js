function getSum(num1, num2) {
    var total = num1 + num2;
    return total;
    console.log("it won't prints this because return works like break");
}
var result=getSum(6,20);
// it won't prints this because return works like break
   